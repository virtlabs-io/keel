# scripts/forbidden_blocking_baseline.txt
#
# Auto-generated by scripts/check_forbidden_blocking.sh --regen-baseline.
# One signature per line: <relpath>:<lineno>:<callname>
#
# Each entry is a grandfathered blocking call in a hot-path file.
# The list is expected to shrink over time as the reactor-driven
# refactors land.  When a blocking call is removed (or annotated
# with NOLINT(keel-blocking) for a verified-safe case), drop the
# matching line from this file.
#
