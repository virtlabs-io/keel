# KEEL Integration Test Suite — Python dependencies
#
# Install into an isolated virtual environment:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

# PostgreSQL adapter
psycopg2-binary>=2.9.9

# Test framework
pytest>=7.0.0

# Test timeout guard (avoids hangs in CI)
pytest-timeout>=2.2.0
